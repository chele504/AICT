# AICT 问卷评分体系（交付版：20 题 / 4 维度 / reverse_order 计分）

本文档对应项目当前默认计分规则（与 `configs/questionnaire_pipeline.json` 中 `scoring.input_value_order=reverse_order`，`dimensions.weight={0.25,0.30,0.25,0.20}` 完全一致），可直接用于游客问卷发放、课题材料或 `process_questionnaire.py` 计分的核对依据。

> 16 题版本历史与说明仍保留在仓库提交历史中，本 v2 与 `AICT_questionnaire_pipeline_20260808` 交付版完全对齐。

## 1. 评分目标

本问卷用于评价 AI 技术在文化和旅游场景中的综合应用成效，重点覆盖以下四个一级指标：

- 技术赋能效能 (tech_empowerment)
- 游客感知体验 (visitor_experience)
- 文化价值传播 (cultural_value)
- 经济社会增值 (economic_social_gain)

问卷结果可用于：

- 形成 `target_score` 监督标签（百分制 0~100，推荐直接用于 AICT 模型训练）
- 支撑专家评价与人工综合评分
- 作为模型训练和效果验证的统一评分依据

## 2. 适用对象

适用于以下被调查者：

- 景区游客
- 博物馆观众
- 数字导览服务使用者
- 智能讲解、问答、推荐系统体验者

## 3. 量表设计（reverse_order 默认模式）

本问卷默认采用 5 级 Likert 量表，问卷星导出时按"选项位置"记录 1~5，需进行 reverse_order 换算。

### 3.1 问卷星前台选项（游客看到）

| 题号 | 选项 1 | 选项 2 | 选项 3 | 选项 4 | 选项 5 |
| --- | --- | --- | --- | --- | --- |
| 所有正向题 | 5 分（非常同意） | 4 分 | 3 分 | 2 分 | 1 分（非常不同意）|

### 3.2 问卷星后台导出的 raw 值（第 i 道题的 `exported_raw_value`，1~5）

| raw | 实际语义（选项） | 换算后的实际分数 |
|---|---|---|
| 1 | 5 分选项（非常同意） | 6 − 1 = 5 |
| 2 | 4 分选项 | 6 − 2 = 4 |
| 3 | 3 分选项（中性）| 6 − 3 = 3 |
| 4 | 2 分选项 | 6 − 4 = 2 |
| 5 | 1 分选项（非常不同意）| 6 − 5 = 1 |

换算公式（代码中 `scoring.input_value_order = "reverse_order"` 默认启用）：

```text
actual_score_i = 6 − exported_raw_value_i   ; 对每道正向题 q1..q20
```

> 说明：本项目所有 20 道题均为正向题，不设置反向题，因此 reverse_order 统一应用于所有量表项。`input_value_order = "direct"` 时将使用 `actual_score_i = exported_raw_value_i`，仅用于与旧数据兼容。

### 3.3 分数含义（换算后）

| 实际分数 | 含义 |
| --- | --- |
| 1 | 体验或认知明显不符合 |
| 2 | 基本不符合 |
| 3 | 感受中性 |
| 4 | 基本符合 |
| 5 | 明显符合 |

## 4. 一级指标与权重（交付版固定）

每维度 5 题，共 20 题；权重与脚本 [questionnaire_pipeline.json](file:///c:/Users/lenovo/Desktop/AICT/configs/questionnaire_pipeline.json) 完全一致。

| 一级指标 | 代码 | 题号 | 维度权重 |
| --- | --- | --- | --- |
| 技术赋能效能 | `tech_empowerment` | q1 ~ q5 | 0.25 |
| 游客感知体验 | `visitor_experience` | q6 ~ q10 | 0.30 |
| 文化价值传播 | `cultural_value` | q11 ~ q15 | 0.25 |
| 经济社会增值 | `economic_social_gain` | q16 ~ q20 | 0.20 |
| 开放题 | `review_text` | q21（或"其他建议"）| N/A → 直接写入 review_text 文本模态 |

说明：

- 权重适合课题原型与实验阶段使用
- 若后续组织专家咨询，可在 JSON 中修改 `dimensions[i].weight`（四维度权重之和必须为 1.0）

## 5. 问卷题项设计（20 题交付版）

### A. 技术赋能效能（q1 ~ q5）

1. 智能导览 / 讲解功能能够有效提升我的参观效率。
2. AI 推荐内容与当前场景较匹配，对我有帮助。
3. 智能问答或交互系统响应及时、使用顺畅。
4. 相关数字化服务明显提升了景区 / 场馆的服务能力。
5. 我认为 AI 技术显著提升了景区 / 场馆的整体服务效率。

维度解释：衡量 AI 系统是否真正改善服务效率、交互质量与场景适配性。

### B. 游客感知体验（q6 ~ q10）

6. AI 技术让我的参观过程更加轻松便捷。
7. 智能服务提升了我的沉浸感和参与感。
8. 我对本次数字化文旅体验整体满意。
9. 如果下次再来，我仍愿意继续使用这些智能服务。
10. 智能设备 / 服务的交互对我来说简单易学。

维度解释：衡量游客主观体验、满意度、沉浸感和持续使用意愿。

### C. 文化价值传播（q11 ~ q15）

11. 智能讲解或推荐让我更容易理解文化内容。
12. AI 服务增强了我对历史、文化或展陈信息的兴趣。
13. 与传统方式相比，数字化服务提升了文化知识获取效果。
14. 本次体验让我感受到更强的文化传播价值和教育意义。
15. 数字化服务促进了我对文化内容的进一步探索欲望。

维度解释：衡量文化理解、知识获取、传播深度与教育价值。

### D. 经济社会增值（q16 ~ q20）

16. 智能化服务提升了我对该景区 / 场馆整体形象的认可度。
17. 本次体验增强了我向他人推荐该景区 / 场馆的意愿。
18. 智能服务提升了我参与相关消费或延伸服务的意愿。
19. 我认为 AI 技术对提升该场景的社会服务价值是有帮助的。
20. 我愿意为该类 AI+文旅 智能服务承担合理的额外成本或溢价。

维度解释：衡量品牌传播、推荐意愿、消费促进与社会影响。

### E. 开放题（review_text，直接入库）

> 您对本次体验 / AI 服务有什么其他建议或感受？

- 文本会直接写入模型的 `review_text` 字段，不参与四维分 / target_score 计算。
- 默认若用户填写文本长度 > 0 且非"满意 / 非常好 / 没有 / 无 / 没有问题 / 还不错 / 一般"等 9 项空语义短语，则视为有效反馈（placeholder_texts 已在配置中列出），在 AI 标注阶段会进行 sentiment / discomfort / confidence / meaningful_feedback 四维度提取。

## 6. 评分计算规则（交付版：与 process_questionnaire.py 完全一致）

### 6.1 步骤 1：对 q1~q20 执行 reverse_order 换算（默认）

对每道题（1 ≤ i ≤ 20）：

```text
actual_score_i = 6 − exported_raw_value_i
```

### 6.2 步骤 2：一级指标得分（百分制）

每个一级指标由 5 个题项组成，计算该维度的平均分（1~5 分），再换算为百分制（0~100）。

计算公式：

```text
维度_平均分 = Σ(该维度内 actual_score) / 5
维度_百分制 = 维度_平均分 × 20
```

**等价公式**：`维度_百分制 = (维度_平均分 / 5) × 100`，两者完全相同。

例如：

- 技术赋能效能 q1~q5 换算后分数为 `4, 5, 4, 3, 4`
- 平均分 = `(4+5+4+3+4) / 5 = 4.0`
- 百分制 = `4.0 × 20 = 80`

### 6.3 步骤 3：问卷总分 = target_score

采用四维度权重加权汇总：

```text
target_score =
0.25 × tech_empowerment +
0.30 × visitor_experience +
0.25 × cultural_value +
0.20 × economic_social_gain
```

最终总分范围为 `0-100`（百分制）。

### 6.4 target_score 的使用

若问卷是当前样本的主要标签来源，则：

```text
target_score = 问卷总分（直接作为模型训练标签）
```

> 默认情况下，**四维度分 + quality_score 不进入模型训练特征列（`include_derived_dimensions=false`）**，仅作为质量审计与报告可视化使用；模型训练特征仅保留 AI 结构化 5 列 + review_text 文本。见 [QUESTIONNAIRE_PIPELINE.md](file:///c:/Users/lenovo/Desktop/AICT/QUESTIONNAIRE_PIPELINE.md) 第四节标签泄漏防护。

## 7. 结果解释标准（等级划分）

建议对总分划分为以下等级（与 HTML 报告中"等级划分占比"饼图颜色一致）：

| 总分区间 | 等级 | 颜色（图表） | 解释 |
| --- | --- | --- | --- |
| 90-100 | 优秀 | #22C55E | AI+文旅应用成效非常突出 |
| 80-89 | 良好 | #3B82F6 | 整体表现较强，具备明显应用价值 |
| 70-79 | 中等 | #F59E0B | 成效较明显，但仍有优化空间 |
| 60-69 | 及格 | #F97316 | 有一定作用，但体验或价值传播仍不足 |
| 0-59 | 待提升 | #EF4444 | 应用成效不明显，需要重点改进 |

> 注意："等级"不参与模型训练，仅用于可视化报告的"等级划分占比"饼图。

## 8. 质量 6 项罚分（quality_score 与 quality_status）

quality_score 是**样本可信度**指标，不参与 target_score 的计算，仅用于数据清洗 / 人工复核队列 / 报告 KPI。

| Flag 名称 | 中文 | 判定规则 | 罚分 |
| --- | --- | --- | --- |
| `flag_short_duration` | 答题时长过短 | `duration_seconds < min_duration_seconds`（默认 60 s） | 15 |
| `flag_straight_line` | 直线答案 | 20 道量表题 actual_score 的 `nunique == 1` | 15 |
| `flag_low_personal_variance` | 个人方差过低 | 同一样本 20 道题的 std < `low_variance_threshold`（默认 0.5） | 10 |
| `flag_duplicate_answer_pattern` | 重复答题向量 | 20 维 actual_score 向量在全数据集中出现 > 1 次 | 10 |
| `flag_duplicate_ip` | 重复 IP | 同一 `ip_group_hash`（脱敏后的 IP 哈希）提交 > 1 次 | 5 |
| `flag_overall_inconsistency` | 总体不一致 | `|actual_score(q20) − mean(q1~q19)| > threshold`（默认 2.0） | 15 |

### 计算：

```text
quality_score = clip(100 − Σ( penalty_i × I(flag_i 为 True) ), 0, 100)
quality_status =
  "pass"     若 quality_score >= 75
  "review"   若 60 <= quality_score < 75
  "invalid"  若 quality_score < 60 或 存在必填缺失（q1~q20 任一为空）
```

> 默认 `quality_score` **不进入**模型训练特征列（include_derived_dimensions=false）；quality_status=invalid 的样本默认不进入 train/test（仅放入 review_queue.csv 做人工复核，质量队列不会被 train/test 读取。

## 9. 数据入库建议（与 AICT v2 标准对齐）

如果你把问卷结果接入项目，建议保存以下字段：

| 字段名 | 类型 | 说明 |
| --- | --- | --- |
| `q1` - `q20` | int | 问卷星原始 raw 值；脚本会自动执行 reverse_order 换算（不要手工转换后再存，便于审计）。 |
| `review_text` | string | 开放题原文。 |
| `tech_empowerment` | float | 一级指标百分制（由脚本计算，可作为审计字段）。 |
| `visitor_experience` | float | 一级指标百分制。 |
| `cultural_value` | float | 一级指标百分制。 |
| `economic_social_gain` | float | 一级指标百分制。 |
| `target_score` | float | 问卷总分，0-100 百分制（直接作为标签使用）。 |
| `quality_score` / `quality_status` | float / string | 样本可信度（脚本可计算）。 |
| `flag_short_duration` ~ `flag_overall_inconsistency` | bool 0/1 | 6 项质量罚分命中情况（脚本可计算）。 |

默认 `aict_dataset.csv` 输出列会精简为 10 列（含 review_text/image_path/audio_path 5 AI 列 + target_score/dataset_split），四维分 / q1..20 / quality_score 不默认输出；若需要保留可在 JSON 中打开 include 开关。

## 10. 推荐发放方式

建议采用以下问卷发放场景：

- 游客完成参观后即时填写（体验越鲜活越真实）
- 体验智能导览 / 问答服务后填写
- 活动结束后统一采集
- 访谈结束后由研究人员辅助录入

建议每份问卷同时记录以下辅助信息（不一定直接入模型，但有助于去噪 / 分层 / 审计）：

- 调查时间（自动，可用作 sample_timestamp）
- 调查地点（可作为 `scene_id` / `scene_type` 分层）
- 场景类型（景区 / 博物馆 / 展演 / 线路）
- 是否使用 AI 导览（二元）
- 是否使用智能问答（二元）
- 是否首次到访（二元）

## 11. 可直接用于论文或课题文本的表述（与交付版对齐）

> 本研究围绕技术赋能效能、游客感知体验、文化价值传播、经济社会增值四个一级维度构建 AI+文旅应用成效问卷评价体系。问卷采用 5 级 Likert 量表，共设置 20 个题项，问卷星按"5、4、3、2、1"顺序排列选项并以选项位置 1~5 导出原始值，换算公式为 `实际分数 = 6 − 导出值`。各一级维度得分通过题项平均值换算为百分制，并按照 0.25、0.30、0.25、0.20 的权重加权汇总形成综合成效得分 `target_score`，用作模型训练标签与应用效果评价依据；同时设置"答题时长、直线答案、低个人方差、重复向量、重复 IP、总体不一致性"6 项质量罚分机制，对可信度低于阈值的样本进入人工复核队列。

## 12. 简版问卷表（20 题交付版）

你后续如果需要发放，可以直接整理成下面这种形式：

| 题号 | 题项 | 5 分 | 4 分 | 3 分 | 2 分 | 1 分 |
| --- | --- | --- | --- | --- | --- | --- |
| Q1 | 智能导览/讲解功能能够有效提升我的参观效率 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q2 | AI 推荐内容与当前场景较匹配，对我有帮助 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q3 | 智能问答或交互系统响应及时、使用顺畅 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q4 | 相关数字化服务明显提升了景区/场馆的服务能力 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q5 | 我认为 AI 技术显著提升了景区/场馆的整体服务效率 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q6 | AI 技术让我的参观过程更加轻松便捷 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q7 | 智能服务提升了我的沉浸感和参与感 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q8 | 我对本次数字化文旅体验整体满意 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q9 | 如果下次再来，我仍愿意继续使用这些智能服务 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q10 | 智能设备/服务的交互对我来说简单易学 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q11 | 智能讲解或推荐让我更容易理解文化内容 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q12 | AI 服务增强了我对历史、文化或展陈信息的兴趣 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q13 | 与传统方式相比，数字化服务提升了文化知识获取效果 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q14 | 本次体验让我感受到更强的文化传播价值和教育意义 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q15 | 数字化服务促进了我对文化内容的进一步探索欲望 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q16 | 智能化服务提升了我对该景区/场馆整体形象的认可度 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q17 | 本次体验增强了我向他人推荐该景区/场馆的意愿 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q18 | 智能服务提升了我参与相关消费或延伸服务的意愿 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q19 | 我认为 AI 技术对提升该场景的社会服务价值是有帮助的 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q20 | 我愿意为该类 AI+文旅智能服务承担合理的额外成本或溢价 | ▢ | ▢ | ▢ | ▢ | ▢ |
| Q21 | 开放题：您对本次体验/AI服务有什么其他建议或感受？ | ___（文本）___ |  |  |  |  |
